name: Daily Operating Selection Refresh

on:
  workflow_dispatch:
    inputs:
      force_run:
        description: 'Run even if no fresh NYSE close is detected'
        type: boolean
        default: false
      latest_run:
        description: 'Run directory restored/used as operating source'
        type: string
        default: 'outputs'
      strict_selection:
        description: 'Fail when data freshness contract blocks operating selection'
        type: boolean
        default: true
  schedule:
    # 10:15 KST Tue-Sat, after crisis, Form4, free-data, and readiness windows.
    - cron: '15 1 * * 2-6'

permissions:
  actions: read
  contents: read

concurrency:
  group: daily-operating-selection-refresh
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: requirements_github.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements_github.txt

      - name: Check latest NYSE close
        id: market
        env:
          FORCE_RUN: ${{ github.event_name == 'workflow_dispatch' && inputs.force_run || 'false' }}
        run: |
          ARGS=(
            --output "$RUNNER_TEMP/daily_market_session_gate.json"
            --min-close-age-minutes 90
            --max-close-age-hours 18
          )
          if [ "${FORCE_RUN:-false}" = "true" ]; then
            ARGS+=(--force)
          fi
          python tools/run_daily_market_session_gate.py "${ARGS[@]}"

      - name: Fetch pinned Run287 policy commit
        if: steps.market.outputs.ready == 'yes'
        run: git fetch --no-tags --depth=1 origin 15176b588d5bb0792bce1df6367758d795a8a33a

      - name: Restore GitHub cache
        if: steps.market.outputs.ready == 'yes'
        uses: actions/cache/restore@v4
        with:
          path: |
            cache_prices
            cache_macro
            models
            feature_store/scored_oos_latest.parquet
            data_raw/free
            data_pit/free
            data_pit/sec
            data_pit/etf_holdings
            data_pit/macro
            manifests/free_data
          key: daily-operating-selection-${{ runner.os }}-${{ github.run_id }}
          restore-keys: |
            daily-operating-selection-${{ runner.os }}-
            collector-cache-${{ runner.os }}-
            replay-price-cache-${{ runner.os }}-

      - name: Restore Run287 research-static archive cache
        if: steps.market.outputs.ready == 'yes'
        id: run287-research-static-cache
        uses: actions/cache/restore@v4
        with:
          path: run287_static_archive/run287_exact_static_archive_v1.zip
          key: run287-research-static-${{ runner.os }}-66ca4b6a6a61cb7e-v1

      - name: Restore validated forward paper state cache
        if: steps.market.outputs.ready == 'yes'
        uses: actions/cache/restore@v4
        with:
          path: |
            outputs/daily_simulated_fill_ledger
            outputs/holding_risk_watch
            outputs/run287_exact_packet_upstream
            outputs/run287_exact_packet_input_sources
            outputs/run287_exact_packet_input_registry
            outputs/run287_decision_observation_archive
            outputs/run287_risk_outcome_archive
            outputs/run287_risk_outcome_price_cache
          key: daily-paper-ledger-${{ runner.os }}-${{ github.run_id }}-${{ github.run_attempt }}
          restore-keys: |
            daily-paper-ledger-${{ runner.os }}-

      - name: Configure rclone
        if: steps.market.outputs.ready == 'yes'
        env:
          GOOGLE_SERVICE_ACCOUNT_KEY: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_KEY }}
          RCLONE_CONFIG_GDRIVE: ${{ secrets.RCLONE_CONFIG_GDRIVE }}
          GDRIVE_ROOT_FOLDER_ID: ${{ vars.GDRIVE_ROOT_FOLDER_ID || secrets.GDRIVE_ROOT_FOLDER_ID }}
        run: |
          set -euo pipefail
          if [ -z "${RCLONE_CONFIG_GDRIVE:-}" ] && [ -z "${GOOGLE_SERVICE_ACCOUNT_KEY:-}" ]; then
            echo "[gdrive] no auth configured; continuing cache/local only"
            echo "GDRIVE_READY=no" >> "$GITHUB_ENV"
            exit 0
          fi
          curl -sLo /tmp/rclone.zip https://downloads.rclone.org/rclone-current-linux-amd64.zip
          unzip -q /tmp/rclone.zip -d /tmp/
          sudo mv /tmp/rclone-*/rclone /usr/local/bin/rclone
          chmod +x /usr/local/bin/rclone
          mkdir -p ~/.config/rclone
          if [ -n "${RCLONE_CONFIG_GDRIVE:-}" ]; then
            echo "$RCLONE_CONFIG_GDRIVE" > ~/.config/rclone/rclone.conf
          else
            echo "$GOOGLE_SERVICE_ACCOUNT_KEY" > /tmp/sa.json
            {
              printf '%s\n' '[gdrive]' 'type = drive' 'scope = drive' 'service_account_file = /tmp/sa.json'
              if [ -n "${GDRIVE_ROOT_FOLDER_ID:-}" ]; then
                printf 'root_folder_id = %s\n' "$GDRIVE_ROOT_FOLDER_ID"
              else
                printf '%s\n' 'shared_with_me = true'
              fi
            } > ~/.config/rclone/rclone.conf
          fi
          echo "GDRIVE_READY=yes" >> "$GITHUB_ENV"

      - name: Restore persistent data and operating outputs
        id: persistent-restore
        if: steps.market.outputs.ready == 'yes'
        env:
          GDRIVE_ROOT_FOLDER_ID: ${{ vars.GDRIVE_ROOT_FOLDER_ID || secrets.GDRIVE_ROOT_FOLDER_ID }}
          GDRIVE_FOLDER_NAME: r1000_top30_institutional
          LATEST_RUN_INPUT: ${{ github.event.inputs.latest_run || 'outputs' }}
        run: |
          set -euo pipefail
          mkdir -p outputs/full_rebuild_logs
          if [ "${GDRIVE_READY:-no}" = "yes" ]; then
            if [ -n "${GDRIVE_ROOT_FOLDER_ID:-}" ]; then
              BASE="gdrive:"
            else
              BASE="gdrive:${GDRIVE_FOLDER_NAME}/"
            fi
            restore_dir() {
              local remote="$1"
              local local_dir="$2"
              mkdir -p "$local_dir"
              if rclone lsf "$BASE$remote" >/dev/null 2>&1; then
                echo "[gdrive] restore $remote -> $local_dir"
                timeout 12m rclone copy "$BASE$remote" "$local_dir/" --transfers 6 --checkers 12 --stats 30s --stats-one-line || true
              else
                echo "[gdrive] missing $remote"
              fi
            }
            restore_dir cache_prices cache_prices
            restore_dir cache_macro cache_macro
            restore_dir models models
            restore_file() {
              local remote="$1"
              local local_file="$2"
              mkdir -p "$(dirname "$local_file")"
              if rclone lsf "$BASE$remote" >/dev/null 2>&1; then
                echo "[gdrive] restore file $remote -> $local_file"
                timeout 6m rclone copyto "$BASE$remote" "$local_file" --stats 30s --stats-one-line || true
              else
                echo "[gdrive] missing file $remote"
              fi
            }
            restore_file feature_store/scored_oos_latest.parquet feature_store/scored_oos_latest.parquet
            restore_dir data_raw/free data_raw/free
            if [ ! -s data_raw/free/sec/company_tickers.json ]; then
              restore_file data_raw/sec/company_tickers.json data_raw/free/sec/company_tickers.json
            fi
            restore_dir data_pit/free data_pit/free
            restore_dir data_pit/sec data_pit/sec
            restore_dir data_pit/etf_holdings data_pit/etf_holdings
            restore_dir data_pit/macro data_pit/macro
            restore_dir manifests/free_data manifests/free_data
            restore_dir outputs outputs
            if [ "${LATEST_RUN_INPUT:-outputs}" != "outputs" ]; then
              restore_dir "$LATEST_RUN_INPUT" "$LATEST_RUN_INPUT"
            fi
          fi
          if [ "${LATEST_RUN_INPUT:-outputs}" != "outputs" ] && [ -d "$LATEST_RUN_INPUT" ]; then
            echo "[source] hydrate outputs/ from requested latest_run=$LATEST_RUN_INPUT"
            rm -rf outputs
            mkdir -p outputs
            cp -a "$LATEST_RUN_INPUT"/. outputs/
          fi
          if [ ! -s outputs/scored_latest.csv ] && [ -d cloud_results/full_rebuild/latest_global_alpha_universe ]; then
            echo "[fallback] hydrate outputs/ from committed cloud_results latest mirror"
            mkdir -p outputs
            cp -a cloud_results/full_rebuild/latest_global_alpha_universe/. outputs/
          fi
          if [ "${GDRIVE_READY:-no}" = "yes" ]; then
            PAPER_REMOTE_CANDIDATE="$RUNNER_TEMP/run287_daily_simulated_fill_ledger_remote"
            PAPER_LOCAL_RECOVERY="$RUNNER_TEMP/run287_daily_simulated_fill_ledger_pre_remote"
            rm -rf "$PAPER_REMOTE_CANDIDATE" "$PAPER_LOCAL_RECOVERY"
            restore_dir paper_archive/run287_daily_simulated_fill_ledger "$PAPER_REMOTE_CANDIDATE"
            if [ -d outputs/daily_simulated_fill_ledger ]; then
              cp -a outputs/daily_simulated_fill_ledger "$PAPER_LOCAL_RECOVERY"
            fi
            if [ -s "$PAPER_REMOTE_CANDIDATE/snapshot_integrity.json" ] && \
               python tools/run287_paper_ledger_integrity.py \
                 --state-dir "$PAPER_REMOTE_CANDIDATE" \
                 --require-integrity; then
              python tools/run287_paper_ledger_integrity.py \
                --state-dir outputs/daily_simulated_fill_ledger \
                --install-source "$PAPER_REMOTE_CANDIDATE"
              echo "[paper-ledger] installed checksum-verified Drive snapshot; pre-install recovery=$PAPER_LOCAL_RECOVERY"
            elif [ -s outputs/daily_simulated_fill_ledger/snapshot_integrity.json ] && \
                 python tools/run287_paper_ledger_integrity.py \
                   --state-dir outputs/daily_simulated_fill_ledger \
                   --require-integrity; then
              echo "[paper-ledger] Drive snapshot was unverified; retained verified cache/local snapshot"
            elif [ ! -d outputs/daily_simulated_fill_ledger ] && [ -d "$PAPER_REMOTE_CANDIDATE" ]; then
              echo "[paper-ledger] one-time legacy restore; ledger semantic validation must migrate it before save"
              mv "$PAPER_REMOTE_CANDIDATE" outputs/daily_simulated_fill_ledger
            else
              echo "[paper-ledger] no checksum-verified remote replacement applied"
            fi
            restore_dir paper_archive/run287_holding_risk_watch outputs/holding_risk_watch
            restore_dir paper_archive/run287_decision_observation_archive outputs/run287_decision_observation_archive
            restore_dir paper_archive/run287_risk_outcome_archive outputs/run287_risk_outcome_archive
            restore_dir paper_archive/run287_risk_outcome_price_cache outputs/run287_risk_outcome_price_cache
          fi

      - name: Restore verified Run287 research-static archive
        if: steps.market.outputs.ready == 'yes'
        id: run287-research-static
        env:
          GDRIVE_ROOT_FOLDER_ID: ${{ vars.GDRIVE_ROOT_FOLDER_ID || secrets.GDRIVE_ROOT_FOLDER_ID }}
          GDRIVE_FOLDER_NAME: r1000_top30_institutional
        run: |
          set -euo pipefail
          ARCHIVE="run287_static_archive/run287_exact_static_archive_v1.zip"
          mkdir -p "$(dirname "$ARCHIVE")" outputs/run287_exact_static_restore
          if [ ! -s "$ARCHIVE" ] && [ "${GDRIVE_READY:-no}" = "yes" ] && command -v rclone >/dev/null 2>&1; then
            if [ -n "${GDRIVE_ROOT_FOLDER_ID:-}" ]; then
              BASE="gdrive:"
            else
              BASE="gdrive:${GDRIVE_FOLDER_NAME}/"
            fi
            timeout 8m rclone copyto \
              "${BASE}research_static/run287_exact_static_archive_v1.zip" \
              "$ARCHIVE" \
              --stats 30s --stats-one-line || true
          fi
          if [ ! -s "$ARCHIVE" ]; then
            echo "[run287-static] verified research archive unavailable; exact upstream will skip"
            echo "ready=no" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          python tools/restore_run287_exact_static_archive.py \
            --archive "$ARCHIVE" \
            --expected-archive-sha256 66ca4b6a6a61cb7e9a3a47e2f6d26aa42f30a9b96a25d07699c6cdeb8faf1d84 \
            --destination-root run287_research_static \
            --output outputs/run287_exact_static_restore/status.json
          echo "ready=yes" >> "$GITHUB_OUTPUT"

      - name: Save Run287 research-static archive cache
        if: steps.market.outputs.ready == 'yes' && steps.run287-research-static-cache.outputs.cache-hit != 'true' && steps.run287-research-static.outputs.ready == 'yes'
        uses: actions/cache/save@v4
        with:
          path: run287_static_archive/run287_exact_static_archive_v1.zip
          key: run287-research-static-${{ runner.os }}-66ca4b6a6a61cb7e-v1

      - name: Record completed market session gate
        if: steps.market.outputs.ready == 'yes'
        run: |
          set -euo pipefail
          mkdir -p outputs/daily_market_session_gate
          cp "$MARKET_SESSION_GATE_FILE" outputs/daily_market_session_gate/session.json

      - name: Refresh current price cache
        if: steps.market.outputs.ready == 'yes'
        run: |
          set -euo pipefail
          mkdir -p outputs/full_rebuild_logs
          BOOKS=()
          [ -s outputs/portfolio_latest.csv ] && BOOKS+=(outputs/portfolio_latest.csv)
          [ -s outputs/concentrated_portfolio_latest.csv ] && BOOKS+=(outputs/concentrated_portfolio_latest.csv)
          if [ "${#BOOKS[@]}" -eq 0 ]; then
            [ -s outputs/reports/main_monthly_weights.csv ] && BOOKS+=(outputs/reports/main_monthly_weights.csv)
            [ -s outputs/reports/concentrated_strategy_holdings.csv ] && BOOKS+=(outputs/reports/concentrated_strategy_holdings.csv)
          fi
          if [ "${#BOOKS[@]}" -gt 0 ]; then
            printf '[daily-operating] price refresh books:\n' | tee outputs/full_rebuild_logs/daily_operating_price_cache_refresh.log
            printf '  %s\n' "${BOOKS[@]}" | tee -a outputs/full_rebuild_logs/daily_operating_price_cache_refresh.log
            timeout 8m python tools/build_replay_price_cache.py \
              --books "${BOOKS[@]}" \
              --output-dir cache_prices \
              --required-tickers SPY QQQ SMH SOXX \
              --refresh-through-date "$LAST_NYSE_SESSION_DATE" \
              --refresh-stale-days 2 \
              --max-scored 0 \
              2>&1 | tee -a outputs/full_rebuild_logs/daily_operating_price_cache_refresh.log || \
              echo "[daily-operating] price cache refresh failed or timed out; continuing to freshness contract" | tee -a outputs/full_rebuild_logs/daily_operating_price_cache_refresh.log
          else
            echo "[daily-operating] missing monthly books; price refresh skipped" | tee outputs/full_rebuild_logs/daily_operating_price_cache_refresh.log
          fi

      - name: Build daily market snapshot
        if: steps.market.outputs.ready == 'yes'
        run: |
          set -euo pipefail
          mkdir -p outputs/full_rebuild_logs data_raw/free/market_snapshot data_pit/free/market_snapshot
          BOOKS=()
          [ -s outputs/portfolio_latest.csv ] && BOOKS+=(--book outputs/portfolio_latest.csv)
          [ -s outputs/concentrated_portfolio_latest.csv ] && BOOKS+=(--book outputs/concentrated_portfolio_latest.csv)
          [ -s outputs/reports/operating_main_target_book.csv ] && BOOKS+=(--book outputs/reports/operating_main_target_book.csv)
          [ -s outputs/reports/operating_concentrated_target_book.csv ] && BOOKS+=(--book outputs/reports/operating_concentrated_target_book.csv)
          timeout 10m python tools/build_daily_market_snapshot.py \
            --price-cache cache_prices \
            "${BOOKS[@]}" \
            --scored outputs/scored_latest.csv \
            --max-scored 250 \
            --required-tickers SPY QQQ SMH SOXX \
            --output-dir outputs/daily_market_snapshot \
            --data-lake-dir data_pit/free/market_snapshot \
            --info-cache data_raw/free/market_snapshot/yf_market_info_cache.csv \
            --refresh-info-days 14 \
            2>&1 | tee outputs/full_rebuild_logs/daily_market_snapshot.log || \
            echo "[daily-operating] daily market snapshot failed or timed out; freshness contract will warn" | tee -a outputs/full_rebuild_logs/daily_market_snapshot.log

      - name: Label restored-target revaluation input
        if: steps.market.outputs.ready == 'yes'
        run: |
          set -euo pipefail
          mkdir -p outputs/full_rebuild_logs outputs/sec_enriched_candidate_replay
          mkdir -p outputs/daily_operating_selection_refresh
          python - <<'PY'
          from pathlib import Path
          import os
          import pandas as pd

          paths = [Path("outputs/portfolio_latest.csv"), Path("outputs/concentrated_portfolio_latest.csv")]
          frames = []
          for path in paths:
              if not path.exists() or path.stat().st_size == 0:
                  continue
              frame = pd.read_csv(path, low_memory=False)
              if frame.empty or "ticker" not in frame.columns:
                  continue
              frame["daily_candidate_source"] = str(path)
              frames.append(frame)
          if not frames:
              raise SystemExit(0)
          out = pd.concat(frames, ignore_index=True, sort=False)
          out["ticker"] = out["ticker"].astype(str).str.upper().str.strip()
          out = out[out["ticker"].ne("") & out["ticker"].ne("NAN")].copy()
          date_cols = [c for c in ["rebalance_date", "feature_date", "as_of_date", "last_trade_date"] if c in out.columns]
          if date_cols:
              parsed = pd.DataFrame({c: pd.to_datetime(out[c], errors="coerce") for c in date_cols})
              row_dates = parsed.max(axis=1)
          else:
              row_dates = pd.Series(pd.NaT, index=out.index)
          out["rebalance_date"] = pd.to_datetime(row_dates, errors="coerce").dt.strftime("%Y-%m-%d")
          out["selection_status"] = "RESTORED_TARGET_REVALUATION_ONLY"
          out["same_close_selector_recomputed"] = False
          out = out.drop_duplicates(subset=["rebalance_date", "ticker"], keep="last")
          output = Path("outputs/daily_operating_selection_refresh/restored_target_revaluation_candidate_book.csv")
          output.parent.mkdir(parents=True, exist_ok=True)
          out.to_csv(output, index=False)
          print(f"[daily-operating] restored target revaluation rows={len(out)} path={output}")
          PY
          CANDIDATE_BOOK="outputs/daily_operating_selection_refresh/restored_target_revaluation_candidate_book.csv"
          if [ -s "$CANDIDATE_BOOK" ]; then
            echo "[daily-operating] building SEC-enriched candidate replay from $CANDIDATE_BOOK" | tee outputs/full_rebuild_logs/daily_sec_enriched_candidate_replay.log
            timeout 8m python tools/run_sec_enriched_candidate_replay.py \
              --candidate-book "$CANDIDATE_BOOK" \
              --output-dir outputs/sec_enriched_candidate_replay \
              2>&1 | tee -a outputs/full_rebuild_logs/daily_sec_enriched_candidate_replay.log || \
              echo "[daily-operating] SEC-enriched candidate replay failed or timed out; continuing with restored artifacts if any" | tee -a outputs/full_rebuild_logs/daily_sec_enriched_candidate_replay.log
          elif [ -s outputs/sec_enriched_candidate_replay/candidate_replay_book_sec_enriched.csv ]; then
            echo "[daily-operating] using restored SEC-enriched candidate replay" | tee outputs/full_rebuild_logs/daily_sec_enriched_candidate_replay.log
          else
            echo "[daily-operating] missing candidate replay book; SEC-enriched candidate replay skipped" | tee outputs/full_rebuild_logs/daily_sec_enriched_candidate_replay.log
          fi

      - name: Build operating target books
        if: steps.market.outputs.ready == 'yes'
        run: |
          set -euo pipefail
          mkdir -p outputs/full_rebuild_logs
          timeout 3m python tools/build_operating_target_books.py \
            --latest-run outputs \
            --price-cache cache_prices \
            --output-dir outputs/reports \
            --require-current-latest-target \
            2>&1 | tee outputs/full_rebuild_logs/daily_operating_target_books.log || \
            echo "[daily-operating] operating target book build failed or timed out; freshness contract will block selection" | tee -a outputs/full_rebuild_logs/daily_operating_target_books.log

      - name: Ensure review-only forward paper bootstrap
        if: steps.market.outputs.ready == 'yes'
        run: |
          set -euo pipefail
          python tools/bootstrap_run287_daily_paper_accounts.py \
            --state-dir outputs/daily_simulated_fill_ledger \
            --price-cache cache_prices \
            --main-target outputs/reports/operating_main_target_book.csv \
            --concentrated-target outputs/reports/operating_concentrated_target_book.csv \
            --as-of-date "$LAST_NYSE_SESSION_DATE" \
            --expected-seed-date 2026-07-13 \
            --starting-capital 100000 \
            --cost-bps 25 \
            --max-fill-lag-days 7 \
            2>&1 | tee outputs/full_rebuild_logs/daily_paper_bootstrap.log

      - name: Require exact completed-session close prices
        if: steps.market.outputs.ready == 'yes'
        run: |
          set -euo pipefail
          python tools/validate_daily_close_prices.py \
            --session-date "$LAST_NYSE_SESSION_DATE" \
            --price-cache cache_prices \
            --target outputs/reports/operating_main_target_book.csv \
            --target outputs/reports/operating_concentrated_target_book.csv \
            --account outputs/daily_simulated_fill_ledger/bootstrap/main_account.json \
            --account outputs/daily_simulated_fill_ledger/bootstrap/concentrated_account.json \
            --state-dir outputs/daily_simulated_fill_ledger \
            --required-ticker SPY \
            --required-ticker QQQ \
            --required-ticker SMH \
            --required-ticker SOXX \
            --output outputs/daily_market_session_gate/close_price_coverage.json \
            2>&1 | tee outputs/full_rebuild_logs/daily_close_price_coverage.log

      - name: Refresh daily macro snapshot
        if: steps.market.outputs.ready == 'yes'
        run: |
          set -euo pipefail
          mkdir -p outputs/full_rebuild_logs data_pit/macro data_raw/free/macro/daily_snapshot
          python tools/macro_daily_snapshot.py \
            --out-dir data_pit/macro \
            2>&1 | tee outputs/full_rebuild_logs/daily_macro_snapshot.log || true
          if [ -s data_pit/macro/latest.json ]; then
            cp data_pit/macro/latest.json data_raw/free/macro/daily_snapshot/latest.json
          fi

      - name: Validate freshness contract
        if: steps.market.outputs.ready == 'yes'
        env:
          STRICT_SELECTION: ${{ inputs.strict_selection }}
        run: |
          set -euo pipefail
          mkdir -p outputs/full_rebuild_logs
          python tools/audit_data_readiness.py \
            --latest-run outputs \
            --price-cache cache_prices \
            --output-dir outputs/data_readiness \
            2>&1 | tee outputs/full_rebuild_logs/daily_operating_data_readiness.log || true
          python tools/run_data_freshness_contract.py \
            --latest-run outputs \
            --price-cache cache_prices \
            --output-dir outputs/data_freshness_contract \
            --require-current-operating-books \
            --source-run-id "${GITHUB_RUN_ID}" \
            --source-commit-sha "${GITHUB_SHA}" \
            --source-branch "${GITHUB_REF_NAME}" \
            --source-artifact-name "daily-operating-selection-refresh-${GITHUB_RUN_ID}" \
            --source-context daily_operating_refresh \
            2>&1 | tee outputs/full_rebuild_logs/data_freshness_contract.log
          python - <<'PY'
          import json
          import os
          from pathlib import Path

          status_path = Path("outputs/data_freshness_contract/status.json")
          status = json.loads(status_path.read_text(encoding="utf-8")) if status_path.exists() else {}
          session_path = Path("outputs/daily_market_session_gate/session.json")
          coverage_path = Path("outputs/daily_market_session_gate/close_price_coverage.json")
          session = json.loads(session_path.read_text(encoding="utf-8")) if session_path.exists() else {}
          coverage = json.loads(coverage_path.read_text(encoding="utf-8")) if coverage_path.exists() else {}
          completed_session_date = os.environ.get("LAST_NYSE_SESSION_DATE", "")
          exact_close_ready = bool(
              session.get("ready") is True
              and session.get("session_date") == completed_session_date
              and coverage.get("status") == "PASS"
              and coverage.get("exact_close_coverage") is True
              and coverage.get("session_date") == completed_session_date
              and int(coverage.get("missing_ticker_count", -1)) == 0
          )
          if not exact_close_ready:
              raise SystemExit("completed-session exact-close gate is not satisfied")
          summary = {
              "schema_version": "daily-operating-selection-refresh-v1",
              "daily_operating_refresh": True,
              "review_only": True,
              "canonical_production_sync": False,
              "live_trading_enabled": False,
              "production_mutation_allowed": False,
              "human_approval_required": True,
              "source_of_truth_level": "GITHUB_ARTIFACT",
              "source_run_id": os.environ.get("GITHUB_RUN_ID", ""),
              "source_commit_sha": os.environ.get("GITHUB_SHA", ""),
              "source_branch": os.environ.get("GITHUB_REF_NAME", ""),
              "source_artifact_name": f"daily-operating-selection-refresh-{os.environ.get('GITHUB_RUN_ID', '')}",
              "market_session_date": completed_session_date,
              "market_session_gate_status": session.get("status", "missing"),
              "exact_close_coverage": exact_close_ready,
              "close_price_coverage_status": coverage.get("status", "missing"),
              "selection_allowed": bool(status.get("selection_allowed", False)),
              "promotion_allowed": bool(status.get("promotion_allowed", False)),
              "selection_status": "RESTORED_TARGET_REVALUATION_ONLY",
              "same_close_selector_recomputed": False,
              "recommendation_status": status.get("recommendation_status", "DO_NOT_USE_REVIEW_REQUIRED"),
              "data_freshness_contract": "outputs/data_freshness_contract/status.json",
              "canonical_output_note": "This workflow syncs review artifacts under research_runs only; it does not update canonical production outputs.",
          }
          out = Path("outputs/daily_operating_selection_refresh")
          out.mkdir(parents=True, exist_ok=True)
          (out / "summary.json").write_text(json.dumps(summary, indent=2, sort_keys=True) + "\n", encoding="utf-8")
          (out / "README.md").write_text(
              "# Daily Operating Selection Refresh\n\n"
              "- review_only: `true`\n"
              "- canonical_production_sync: `false`\n"
              "- live_trading_enabled: `false`\n"
              "- production_mutation_allowed: `false`\n"
              "- human_approval_required: `true`\n"
              "- source_of_truth_level: `GITHUB_ARTIFACT`\n",
              encoding="utf-8",
          )
          # P2 owns the final fail-closed selection decision.  Data freshness
          # is recorded here, but a restored target can never authorize a new
          # paper order or set same_close_selector_recomputed=true.
          PY

      - name: Evaluate single promotion and rollback gate
        if: steps.market.outputs.ready == 'yes'
        run: |
          set -euo pipefail
          mkdir -p outputs/full_rebuild_logs
          python tools/run_run287_promotion_gate.py \
            --contract data_static/run287_promotion_gate_contract.json \
            --state data_static/run287_promotion_state.json \
            --evidence data_static/run287_promotion_evidence_current.json \
            --latest-run outputs \
            --output-dir outputs/run287_promotion_gate \
            2>&1 | tee outputs/full_rebuild_logs/daily_run287_promotion_gate.log

      - name: Build operating review outputs
        id: operating_review
        if: steps.market.outputs.ready == 'yes'
        run: |
          set -euo pipefail
          mkdir -p outputs/full_rebuild_logs
          PAPER_AS_OF="$LAST_NYSE_SESSION_DATE"
          RISK_AVAILABLE_FROM="$(date -u +'%Y-%m-%dT%H:%M:%SZ')"
          DATE_TOKEN="${PAPER_AS_OF//-/}"
          SAME_CLOSE_DIR="outputs/run287_same_close_decision/${DATE_TOKEN}"
          # First settle prior next-close orders and freeze the exact-close
          # account mark.  This pass is transaction-safe but may not create a
          # new preview or order from a restored target.
          python tools/run_daily_simulated_fill_ledger.py \
            --state-dir outputs/daily_simulated_fill_ledger \
            --price-cache cache_prices \
            --order-preview-root outputs/account_ledger_preview \
            --main-bootstrap-account outputs/daily_simulated_fill_ledger/bootstrap/main_account.json \
            --concentrated-bootstrap-account outputs/daily_simulated_fill_ledger/bootstrap/concentrated_account.json \
            --main-target outputs/reports/operating_main_target_book.csv \
            --concentrated-target outputs/reports/operating_concentrated_target_book.csv \
            --as-of-date "$PAPER_AS_OF" \
            --decision-time-utc "$RISK_AVAILABLE_FROM" \
            --security-lifecycle-events data_static/run287_exact_packet/security_lifecycle_events.csv \
            --cost-bps 25 \
            --max-fill-lag-days 7 \
            --suppress-new-orders \
            2>&1 | tee outputs/full_rebuild_logs/daily_simulated_fill_ledger_mark_only.log
          python tools/build_run287_holding_risk_watch.py \
            --main-account outputs/daily_simulated_fill_ledger/main/account_state_latest.json \
            --concentrated-account outputs/daily_simulated_fill_ledger/concentrated/account_state_latest.json \
            --price-cache cache_prices \
            --as-of-date "$PAPER_AS_OF" \
            --available-from "$RISK_AVAILABLE_FROM" \
            --output-dir outputs/holding_risk_watch \
            --require-exact-close \
            2>&1 | tee outputs/full_rebuild_logs/daily_holding_risk_watch.log
          export SEC_USER_AGENT="${SEC_USER_AGENT:-R1000QuantEngine research andrewcha231@gmail.com}"
          if python tools/run_run287_exact_packet_upstream.py \
            --valuation-date "$PAPER_AS_OF" \
            --decision-time-utc "$RISK_AVAILABLE_FROM" \
            --attempt-id "${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}" \
            --allow-network \
            2>&1 | tee outputs/full_rebuild_logs/daily_run287_exact_packet_upstream.log; then
            echo "[daily-operating] bounded exact-packet upstream completed or safely skipped"
          else
            echo "[daily-operating] bounded exact-packet upstream blocked; registry will fail closed"
          fi
          if python tools/build_run287_exact_packet_input_registry.py \
            --source-bundle outputs/run287_exact_packet_input_sources/source_bundle.json \
            --valuation-date "$PAPER_AS_OF" \
            --output-dir outputs/run287_exact_packet_input_registry \
            --allow-missing \
            2>&1 | tee outputs/full_rebuild_logs/daily_run287_exact_packet_input_registry.log; then
            echo "[daily-operating] exact packet input registry completed or safely skipped"
          else
            echo "[daily-operating] exact packet input registry blocked; producer will fail closed"
          fi
          if python tools/run_run287_exact_packet_producer.py \
            --input-registry outputs/run287_exact_packet_input_registry/registry.json \
            --valuation-date "$PAPER_AS_OF" \
            --holding-watch-summary outputs/holding_risk_watch/summary.json \
            --holding-watch-csv outputs/holding_risk_watch/holding_risk_watch.csv \
            --output-dir outputs/run287_exact_packet_producer \
            --packet-root outputs \
            --allow-missing \
            2>&1 | tee outputs/full_rebuild_logs/daily_run287_exact_packet_producer.log; then
            echo "[daily-operating] exact packet producer completed"
          else
            echo "[daily-operating] exact packet producer blocked; archive will fail closed or skip"
          fi
          SAME_CLOSE_READY=no
          if python tools/build_run287_same_close_target_books.py \
            --producer-status outputs/run287_exact_packet_producer/status.json \
            --valuation-date "$PAPER_AS_OF" \
            --output-dir "$SAME_CLOSE_DIR" \
            2>&1 | tee outputs/full_rebuild_logs/daily_run287_same_close_target_books.log; then
            SAME_CLOSE_READY=yes
            # A second same-session transaction is permitted only after the
            # suppressed mark and only when the target identity changed.  It
            # enqueues review-only orders eligible for the next NYSE close.
            python tools/run_daily_simulated_fill_ledger.py \
              --state-dir outputs/daily_simulated_fill_ledger \
              --price-cache cache_prices \
              --order-preview-root outputs/account_ledger_preview \
              --main-bootstrap-account outputs/daily_simulated_fill_ledger/bootstrap/main_account.json \
              --concentrated-bootstrap-account outputs/daily_simulated_fill_ledger/bootstrap/concentrated_account.json \
              --main-target "$SAME_CLOSE_DIR/same_close_main_target_book.csv" \
              --concentrated-target "$SAME_CLOSE_DIR/same_close_concentrated_target_book.csv" \
              --main-publish-target outputs/reports/operating_main_target_book.csv \
              --concentrated-publish-target outputs/reports/operating_concentrated_target_book.csv \
              --as-of-date "$PAPER_AS_OF" \
              --decision-time-utc "$RISK_AVAILABLE_FROM" \
              --security-lifecycle-events data_static/run287_exact_packet/security_lifecycle_events.csv \
              --cost-bps 25 \
              --max-fill-lag-days 7 \
              2>&1 | tee outputs/full_rebuild_logs/daily_simulated_fill_ledger.log
          else
            echo "[daily-operating] same-close selector blocked; prior fills/marks retained and zero new orders generated"
          fi
          export SAME_CLOSE_READY
          export SAME_CLOSE_DIR
          python - <<'PY'
          import json
          import os
          from pathlib import Path

          summary_path = Path("outputs/daily_operating_selection_refresh/summary.json")
          summary = json.loads(summary_path.read_text(encoding="utf-8")) if summary_path.is_file() else {}
          status_path = Path(os.environ["SAME_CLOSE_DIR"]) / "status.json"
          status = json.loads(status_path.read_text(encoding="utf-8")) if status_path.is_file() else {}
          ready = os.environ.get("SAME_CLOSE_READY") == "yes" and status.get("status") == "READY_SAME_CLOSE_PAPER_TARGETS"
          summary.update(
              selection_status=status.get("status", "BLOCKED_SAME_CLOSE_SELECTOR"),
              same_close_selector_recomputed=bool(ready and status.get("same_close_selector_recomputed") is True),
              selector_decision_time_utc=status.get("selector_decision_time_utc"),
              target_effective_date=status.get("target_effective_date"),
              order_eligible_close_date=status.get("order_eligible_close_date"),
              same_close_decision_status=str(status_path),
              new_order_generation_allowed=bool(ready),
              live_trading_enabled=False,
              production_mutation_allowed=False,
          )
          summary_path.write_text(json.dumps(summary, indent=2, sort_keys=True) + "\n", encoding="utf-8")
          PY
          ARCHIVE_ARGS=(
            --discover-root outputs/run287_exact_packet_discovery_disabled
            --allow-missing
            --valuation-date "$PAPER_AS_OF"
            --expected-contract-sha256 7cb0bd4a86493ae861f32338f13ff17fbf3997fc23657381d79005b750cfbdc2
            --output-dir outputs/run287_decision_observation_archive
          )
          if python -c 'import json; from pathlib import Path; p=Path("outputs/run287_exact_packet_producer/status.json"); x=json.loads(p.read_text()) if p.is_file() else {}; raise SystemExit(0 if x.get("exact_packet_ready") is True else 1)'; then
            ARCHIVE_ARGS+=(
              --selector-manifest "outputs/run287_current_selector_no_write_exact_close_${DATE_TOKEN}/manifest.json"
              --candidate-risk-summary "outputs/run287_candidate_risk_watch_exact_close_${DATE_TOKEN}/summary.json"
            )
          fi
          python tools/archive_run287_decision_observation.py "${ARCHIVE_ARGS[@]}" \
            2>&1 | tee outputs/full_rebuild_logs/daily_run287_decision_observation_archive.log
          RISK_OUTCOME_ARGS=(
            --decision-archive outputs/run287_decision_observation_archive
            --price-cache outputs/run287_risk_outcome_price_cache
            --output-dir outputs/run287_risk_outcome_archive
            --as-of-date "$PAPER_AS_OF"
          )
          python tools/resolve_run287_risk_outcomes.py "${RISK_OUTCOME_ARGS[@]}" \
            2>&1 | tee outputs/full_rebuild_logs/daily_run287_risk_outcomes.log
          if [ -s outputs/run287_risk_outcome_archive/price_universe.csv ]; then
            timeout 8m python tools/build_replay_price_cache.py \
              --books outputs/run287_risk_outcome_archive/price_universe.csv \
              --output-dir outputs/run287_risk_outcome_price_cache \
              --start 2026-07-13 \
              --required-tickers SPY \
              --refresh-stale-days 2 \
              --max-scored 0 \
              --max-tickers 150 \
              2>&1 | tee -a outputs/full_rebuild_logs/daily_run287_risk_outcomes.log
            python tools/resolve_run287_risk_outcomes.py "${RISK_OUTCOME_ARGS[@]}" \
              2>&1 | tee -a outputs/full_rebuild_logs/daily_run287_risk_outcomes.log
          fi
          python tools/run_operating_snapshot.py --latest-run outputs --output-dir outputs/operating_snapshot 2>&1 | tee outputs/full_rebuild_logs/daily_operating_snapshot.log || true
          python tools/run_user_portfolio_reports.py --latest-run outputs --price-cache cache_prices --output-dir outputs/user_portfolio_reports 2>&1 | tee outputs/full_rebuild_logs/daily_user_portfolio_reports.log || true
          python tools/run_user_current_report.py --latest-run outputs --price-cache cache_prices --output-dir outputs/user_current --strict 2>&1 | tee outputs/full_rebuild_logs/daily_user_current_report.log || true
          mkdir -p outputs/user_current
          cat > outputs/user_current/DAILY_REVIEW_ONLY.md <<'EOF'
          # Daily Review-Only Current Output

          - daily_operating_refresh: `true`
          - review_only: `true`
          - canonical_production_sync: `false`
          - live_trading_enabled: `false`
          - production_mutation_allowed: `false`
          - human_approval_required: `true`
          - source_of_truth_level: `GITHUB_ARTIFACT`

          This directory was produced by `daily_operating_selection_refresh.yml`
          and is synced under `research_runs/`. It must not be treated as a
          canonical production output without a separate user-approved promotion.

          Required review-only user contract files:
          - `outputs/user_current/02_target_weights.csv`
          - `outputs/user_current/03_order_preview.csv`
          - `outputs/user_current/08_rebalance_decision.json`
          EOF
          python tools/build_daily_user_current_contract.py \
            --latest-run outputs \
            --output-dir outputs/user_current \
            --source-run-id "${GITHUB_RUN_ID}" \
            --source-commit-sha "${GITHUB_SHA}" \
            --source-branch "${GITHUB_REF_NAME}" \
            --source-artifact-name "daily-operating-selection-refresh-${GITHUB_RUN_ID}" \
            2>&1 | tee outputs/full_rebuild_logs/daily_user_current_contract.log || true

      - name: Upload daily operating evidence artifact
        if: always() && steps.market.outputs.ready == 'yes'
        uses: actions/upload-artifact@v4
        with:
          name: daily-operating-selection-refresh-${{ github.run_id }}
          path: |
            outputs/data_freshness_contract/
            outputs/daily_market_snapshot/
            outputs/daily_market_session_gate/
            outputs/daily_operating_selection_refresh/
            outputs/data_readiness/
            outputs/reports/operating_target_books_*
            outputs/holding_risk_watch/
            outputs/run287_exact_packet_upstream/
            outputs/run287_exact_packet_input_sources/
            outputs/run287_exact_packet_input_registry/
            outputs/run287_exact_packet_producer/
            outputs/run287_same_close_decision/*/status.json
            outputs/run287_current_selector_no_write_exact_close_*/
            outputs/run287_candidate_risk_watch_exact_close_*/
            outputs/run287_decision_observation_archive/
            outputs/run287_risk_outcome_archive/
            outputs/run287_promotion_gate/
            outputs/run287_risk_outcome_price_cache/replay_price_cache_manifest.json
            outputs/sec_enriched_candidate_replay/
            outputs/portfolio_latest.csv
            outputs/concentrated_portfolio_latest.csv
            data_pit/macro/latest.json
            data_raw/free/macro/daily_snapshot/latest.json
            outputs/full_rebuild_logs/daily_*.log
            data_pit/free/market_snapshot/
            data_raw/free/market_snapshot/
            outputs/full_rebuild_logs/daily_user_current_contract.log
            outputs/full_rebuild_logs/daily_paper_bootstrap.log
            outputs/full_rebuild_logs/daily_simulated_fill_ledger_mark_only.log
            outputs/full_rebuild_logs/daily_holding_risk_watch.log
            outputs/full_rebuild_logs/daily_run287_exact_packet_upstream.log
            outputs/full_rebuild_logs/daily_run287_exact_packet_input_registry.log
            outputs/full_rebuild_logs/daily_run287_exact_packet_producer.log
            outputs/full_rebuild_logs/daily_run287_same_close_target_books.log
            outputs/full_rebuild_logs/daily_run287_decision_observation_archive.log
            outputs/full_rebuild_logs/daily_run287_risk_outcomes.log
            outputs/full_rebuild_logs/daily_run287_promotion_gate.log
            outputs/full_rebuild_logs/data_freshness_contract.log
            cache_prices/replay_price_cache_manifest.json
          retention-days: 45

      - name: Verify transactional forward paper snapshot
        if: success() && steps.market.outputs.ready == 'yes'
        run: |
          set -euo pipefail
          python tools/run287_paper_ledger_integrity.py \
            --state-dir outputs/daily_simulated_fill_ledger \
            --require-integrity \
            --output outputs/full_rebuild_logs/daily_paper_snapshot_integrity.json

      - name: Upload accepted paper transaction artifact
        if: success() && steps.market.outputs.ready == 'yes' && steps.operating_review.outcome == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: accepted-paper-transaction-${{ github.run_id }}
          path: |
            outputs/reports/operating_*_target_book.csv
            outputs/operating_snapshot/
            outputs/user_current/
            outputs/user_portfolio_reports/
            outputs/account_ledger_preview/
            outputs/daily_simulated_fill_ledger/
            outputs/run287_same_close_decision/
            outputs/full_rebuild_logs/daily_paper_snapshot_integrity.json
          retention-days: 45

      - name: Save validated forward paper state cache
        if: success() && steps.market.outputs.ready == 'yes'
        uses: actions/cache/save@v4
        with:
          path: |
            outputs/daily_simulated_fill_ledger
            outputs/holding_risk_watch
            outputs/run287_exact_packet_upstream
            outputs/run287_exact_packet_input_sources
            outputs/run287_exact_packet_input_registry
            outputs/run287_decision_observation_archive
            outputs/run287_risk_outcome_archive
            outputs/run287_risk_outcome_price_cache
          key: daily-paper-ledger-${{ runner.os }}-${{ github.run_id }}-${{ github.run_attempt }}

      - name: Sync daily operating artifact to Google Drive
        if: always()
        env:
          GDRIVE_ROOT_FOLDER_ID: ${{ vars.GDRIVE_ROOT_FOLDER_ID || secrets.GDRIVE_ROOT_FOLDER_ID }}
          GDRIVE_FOLDER_NAME: r1000_top30_institutional
        run: |
          set -euo pipefail
          if [ "${GDRIVE_READY:-no}" != "yes" ] || ! command -v rclone >/dev/null 2>&1; then
            echo "[gdrive] not configured; skip daily operating sync"
            exit 0
          fi
          if [ -n "${GDRIVE_ROOT_FOLDER_ID:-}" ]; then
            BASE="gdrive:"
          else
            BASE="gdrive:${GDRIVE_FOLDER_NAME}/"
          fi
          SAFE_BRANCH="$(printf '%s' "${GITHUB_REF_NAME:-daily}" | tr '/:@ ' '____' | tr -cd 'A-Za-z0-9._-')"
          DEST="${BASE}research_runs/${SAFE_BRANCH}/${GITHUB_RUN_ID}/daily_operating_selection_refresh"
          for d in outputs/data_freshness_contract outputs/daily_market_snapshot outputs/daily_operating_selection_refresh outputs/data_readiness outputs/holding_risk_watch outputs/run287_exact_packet_upstream outputs/run287_exact_packet_input_sources outputs/run287_exact_packet_input_registry outputs/run287_exact_packet_producer outputs/run287_decision_observation_archive outputs/run287_risk_outcome_archive outputs/run287_promotion_gate outputs/sec_enriched_candidate_replay data_pit/free/market_snapshot data_raw/free/market_snapshot data_pit/macro data_raw/free/macro/daily_snapshot; do
            if [ -d "$d" ]; then
              rclone copy "$d" "$DEST/$d/" --transfers 4 --checkers 8 --stats 30s --stats-one-line || true
            fi
          done
          if [ -s cache_prices/replay_price_cache_manifest.json ]; then
            rclone copyto cache_prices/replay_price_cache_manifest.json "$DEST/cache_prices/replay_price_cache_manifest.json" || true
          fi

      - name: Sync accepted paper transaction to Google Drive
        if: success() && steps.market.outputs.ready == 'yes' && steps.operating_review.outcome == 'success'
        env:
          GDRIVE_ROOT_FOLDER_ID: ${{ vars.GDRIVE_ROOT_FOLDER_ID || secrets.GDRIVE_ROOT_FOLDER_ID }}
          GDRIVE_FOLDER_NAME: r1000_top30_institutional
        run: |
          set -euo pipefail
          if [ "${GDRIVE_READY:-no}" != "yes" ] || ! command -v rclone >/dev/null 2>&1; then
            echo "[gdrive] not configured; accepted paper transaction remains in GitHub artifact/cache"
            exit 0
          fi
          if [ -n "${GDRIVE_ROOT_FOLDER_ID:-}" ]; then
            BASE="gdrive:"
          else
            BASE="gdrive:${GDRIVE_FOLDER_NAME}/"
          fi
          SAFE_BRANCH="$(printf '%s' "${GITHUB_REF_NAME:-daily}" | tr '/:@ ' '____' | tr -cd 'A-Za-z0-9._-')"
          DEST="${BASE}research_runs/${SAFE_BRANCH}/${GITHUB_RUN_ID}/daily_operating_selection_refresh"
          for d in outputs/operating_snapshot outputs/user_current outputs/user_portfolio_reports outputs/account_ledger_preview outputs/daily_simulated_fill_ledger outputs/run287_same_close_decision; do
            test -d "$d"
            rclone copy "$d" "$DEST/$d/" --transfers 4 --checkers 8 --stats 30s --stats-one-line
          done
          for f in outputs/reports/operating_main_target_book.csv outputs/reports/operating_concentrated_target_book.csv; do
            test -s "$f"
            rclone copyto "$f" "$DEST/$f"
          done

      - name: Persist validated forward paper ledger state
        if: success() && steps.market.outputs.ready == 'yes'
        env:
          GDRIVE_ROOT_FOLDER_ID: ${{ vars.GDRIVE_ROOT_FOLDER_ID || secrets.GDRIVE_ROOT_FOLDER_ID }}
          GDRIVE_FOLDER_NAME: r1000_top30_institutional
        run: |
          set -euo pipefail
          if [ "${GDRIVE_READY:-no}" != "yes" ] || ! command -v rclone >/dev/null 2>&1; then
            echo "[gdrive] not configured; validated paper state remains in the GitHub cache/artifact"
            exit 0
          fi
          if [ -n "${GDRIVE_ROOT_FOLDER_ID:-}" ]; then
            BASE="gdrive:"
          else
            BASE="gdrive:${GDRIVE_FOLDER_NAME}/"
          fi
          test -s outputs/daily_simulated_fill_ledger/summary.json
          test -s outputs/daily_simulated_fill_ledger/snapshot_integrity.json
          test -s outputs/holding_risk_watch/summary.json
          python tools/run287_paper_ledger_integrity.py \
            --state-dir outputs/daily_simulated_fill_ledger \
            --require-integrity
          RECOVERY="${BASE}paper_archive/recovery/${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}/run287_daily_simulated_fill_ledger"
          CANONICAL="${BASE}paper_archive/run287_daily_simulated_fill_ledger"
          rclone copy outputs/daily_simulated_fill_ledger "$RECOVERY/" --transfers 2 --checkers 4 --stats 30s --stats-one-line
          rclone check outputs/daily_simulated_fill_ledger "$RECOVERY/" --checksum --one-way
          rclone sync outputs/daily_simulated_fill_ledger "$CANONICAL/" --transfers 2 --checkers 4 --stats 30s --stats-one-line
          rclone check outputs/daily_simulated_fill_ledger "$CANONICAL/" --checksum --one-way
          rclone copy outputs/holding_risk_watch "${BASE}paper_archive/run287_holding_risk_watch/" --transfers 2 --checkers 4 --stats 30s --stats-one-line
          if [ -s outputs/run287_decision_observation_archive/manifest.json ]; then
            rclone copy outputs/run287_decision_observation_archive "${BASE}paper_archive/run287_decision_observation_archive/" --transfers 2 --checkers 4 --stats 30s --stats-one-line
          fi
          if [ -s outputs/run287_risk_outcome_archive/summary.json ]; then
            rclone copy outputs/run287_risk_outcome_archive "${BASE}paper_archive/run287_risk_outcome_archive/" --transfers 2 --checkers 4 --stats 30s --stats-one-line
          fi
          if [ -d outputs/run287_risk_outcome_price_cache ]; then
            rclone copy outputs/run287_risk_outcome_price_cache "${BASE}paper_archive/run287_risk_outcome_price_cache/" --transfers 2 --checkers 4 --stats 30s --stats-one-line
          fi

      - name: Save refreshed GitHub cache
        if: always() && steps.market.outputs.ready == 'yes'
        uses: actions/cache/save@v4
        with:
          path: |
            cache_prices
            cache_macro
            models
            feature_store/scored_oos_latest.parquet
            data_raw/free
            data_pit/free
            data_pit/sec
            data_pit/etf_holdings
            data_pit/macro
            manifests/free_data
          key: daily-operating-selection-${{ runner.os }}-${{ github.run_id }}
